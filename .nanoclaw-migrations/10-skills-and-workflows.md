# 10 — Skill edits + workflow deletions

## Skill edits to upstream skills

These are TINY tweaks (<60 lines total). The diffs:

| Skill | Insertions | Deletions | What |
|---|---|---|---|
| `setup/SKILL.md` | 0 | 6 | Removed something — diff to find |
| `setup/diagnostics.md` | 0 | 50 | Trimmed diagnostics |
| `update-nanoclaw/SKILL.md` | 0 | 6 | Same |
| `update-nanoclaw/diagnostics.md` | 0 | 50 | Same |

**Action**: re-diff against v2's versions of these skills. v2's setup skill diverged significantly (16 commits in `.claude/skills/new-setup/`). Most or all of our trimmed lines are likely already restructured in v2 — **probably skip this entirely** unless the v2 versions still have the offending lines we removed.

To find what we removed: `git diff <BASE>..HEAD -- .claude/skills/setup/SKILL.md` then check whether v2's main has those lines. If yes, re-apply the same trim. If no, drop the customization.

## Original skills we contributed (NEW files, no upstream collision)

- `.claude/skills/add-google-workspace/SKILL.md` (175 lines)
- `.claude/skills/add-home-assistant/SKILL.md` (69 lines)
- `.claude/skills/add-playwright/SKILL.md` (75 lines)
- `.claude/skills/add-whatsapp/SKILL.md` — modified (we trimmed/edited — verify diff)

The first three move to their respective mzenko fork repos as v2-style installable skills (per architectural decision: keep separate). They install the corresponding sidecar.

`add-whatsapp` should be replaced by v2's whatsapp adapter install path (see `05-channel-other.md`).

## Workflow deletions

We DELETED two CI workflows from upstream:
- `.github/workflows/bump-version.yml` (33 lines deleted)
- `.github/workflows/update-tokens.yml` (43 lines deleted)

**Why**: We don't auto-bump versions on our fork (we're not publishing). The token-counting workflow was upstream-specific maintenance noise.

**Re-apply in v2**: After cloning v2, delete these two workflow files again. They're regenerated by upstream merges otherwise.

## Workflow we should KEEP and re-evaluate

Two workflow files in our diff that are NEW (we added them — not in upstream at base):
- `.github/workflows/fork-sync-skills.yml`
- `.github/workflows/merge-forward-skills.yml`

These appear to be fork-maintenance workflows. Review them; decide whether to keep on v2 (likely yes — fork maintenance is even more relevant once we're back in sync with upstream).

## Diagnostics file modifications

The 50-line trims in `*/diagnostics.md` files are likely removing upstream-specific diagnostic steps that don't apply to our setup. Same approach as SKILL.md tweaks: check if v2's diagnostics still have the offending content. If not, skip.
